parameters:
  ascii:
    failed: config/lint/grumpy.txt
    succeeded: config/lint/happy.txt
  git_dir: .
  bin_dir: vendor/bin
  tasks:
    phplint: ~
    yamllint: ~
    composer: ~
    jsonlint: ~
    phpcs:
      standard: Drupal
imports:
  - { resource: vendor/drupol/drupal-conventions/config/drupal8/grumphp.yml }
